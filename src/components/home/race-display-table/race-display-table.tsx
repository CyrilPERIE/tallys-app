import { cn } from "@/lib/utils"

export const RaceDisplayTable = ({ className }: { className?: string }) => {
    return (
        <div className={cn("", className)}>
            RaceDisplayTable
        </div>
    )
}
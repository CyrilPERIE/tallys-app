import { cn } from "@/lib/utils"

export const StatsCumulativeProfit = ({ className }: { className?: string }) => {
    return (
        <div className={cn("", className)}>
            Cumulative Profit
        </div>
    )
}
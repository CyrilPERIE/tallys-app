import { cn } from "@/lib/utils";

export const StatsROI = ({ className }: { className?: string }) => {
    return (
        <div className={cn("", className)}>
            <p>StatsROI</p>
        </div>
    )
}